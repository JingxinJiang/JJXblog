
<html>

<%- include('./partials/header') %>

<body>
    <%- include('./partials/nav') %>
    <% const name='Lucy' %>
    
    <div class="content">
        <h1>Welcome to <%=name%> first page!</h1>
        <br /><br />
        
        <div class="blog-content">
            <% if (blogs.length>0){blogs.forEach((blog)=>{%>
              <a href="/all-blogs/<%= blog.id %>"><h2><%= blog.title%></h2></a>
                <p><%= blog.snippet%></p>
            <% })}else { %>       
            <p><b>There is no blogs</b></p>
            <%}%>
        </div>
    </div>
    <!-- <%- include('./partials/footer')%> -->
</body>

</html>